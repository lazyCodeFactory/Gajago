package com.ssocio.sharing.cmmn.web;

import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

public class WebBaseController extends WebAbstractController {
	// --------------------------------------------------------------------------
		// # Log 설정
		// --------------------------------------------------------------------------
		private static final Logger logger = LoggerFactory.getLogger(WebBaseController.class);

		//  public boolean isLogin(HttpServletRequest request) {
		//
//				SessionLoginCheck sessionCheck = new SessionLoginCheck();
//				return sessionCheck.isLogin(request) ;
		//  }
		//
		//  public BaseUserInfo getLoginInfo(HttpServletRequest request, BaseUserInfo userinfo)
		//  {
//		    HttpSession session = request.getSession(false);
//		    if(session != null)
//		      return (BaseUserInfo)session.getAttribute("userInfo");
//		    else
//		      return null;
		//  }
}
